var assert = require('chai').assert;

// var server = require('../src/js/server-app.js');

// describe('server', function () {
//   before(function () {
//     server.listen(8000);
//   });

//   after(function () {
//     server.close();
//   });
// });

describe('luckychain', function() {
  it('transaction into block into chain', function() {
    // var tx = ;
    // addTransactionTestingOnly(tx);
    // interval();

    assert.equal(-1, [1,2,3].indexOf(5));
  });
});